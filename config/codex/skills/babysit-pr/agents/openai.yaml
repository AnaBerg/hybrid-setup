interface:
  display_name: "Babysit PR"
  short_description: "Monitor PRs and apply validated fixes"
  default_prompt: "Use $babysit-pr to monitor these GitHub pull requests and apply only evidence-backed fixes."
policy:
  allow_implicit_invocation: false
